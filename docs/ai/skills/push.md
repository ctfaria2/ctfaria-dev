# Push Skill

Push changes to remote repository.

## Rules

1. Always confirm branch before pushing
2. Use `git push -u origin <branch>` for new branches
3. Never force push to main without explicit permission
